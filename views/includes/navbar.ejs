<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <!-- Search Bar - Centered -->
        <div class="navbar-search-centered d-none d-lg-flex">
            <div class="search-wrapper">
                <input 
                    type="text" 
                    class="search-input" 
                    id="serviceSearch" 
                    placeholder="Search for services (cleaning, plumbing, electrician...)"
                    autocomplete="off"
                >
                <i class="fas fa-search search-icon"></i>
            </div>
        </div>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link" href="/services">
                        <i class="fas fa-tools"></i> Services
                    </a>
                </li>
                
                <% if (currUser) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/bookings/history">
                            <i class="fas fa-history"></i> My Bookings
                        </a>
                    </li>
                    
                    <!-- Admin Link (only for admins) -->
                    <% if (currUser.role === 'admin') { %>
                        <li class="nav-item">
                            <a class="nav-link position-relative" href="/admin/dashboard">
                                <i class="fas fa-tachometer-alt"></i> Admin Panel
                                <span class="badge bg-danger ms-1 pending-badge" id="pendingBadge" style="display: none; position: absolute; top: 0; right: -10px; font-size: 0.7rem;">0</span>
                            </a>
                        </li>
                    <% } %>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle"></i> <%= currUser.username %>
                            <% if (currUser.role === 'admin') { %>
                                <span class="badge bg-primary ms-1" style="font-size: 0.7rem;">Admin</span>
                            <% } %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/logout">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">
                            <i class="fas fa-sign-in-alt"></i> Login
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn-signup" href="/signup">
                            <i class="fas fa-user-plus"></i> Sign Up
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
